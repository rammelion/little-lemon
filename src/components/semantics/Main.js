import Container from "../Container"

export default function Main(props) {
    return (
        <main>
            <Container>
                <p>
                    Test Container
                </p>
            </Container>
        </main>
    )
}