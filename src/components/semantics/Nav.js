import Logo from "../Logo"
import NavMenu from "../NavMenu"

export default function Nav() {
    return (
        <nav>
            <Logo location='nav' />
            <NavMenu />
        </nav>
    )
}